{
    "LIST": {
        "summary": "Returns all blessed keys with their bless flags.",
        "complexity": "O(N) where N is the number of blessed keys.",
        "group": "generic",
        "since": "8.10.0",
        "arity": 2,
        "container": "BLESS",
        "function": "blessCommand",
        "command_flags": [
            "READONLY"
        ],
        "acl_categories": [
            "KEYSPACE"
        ],
        "reply_schema": {
            "type": "object",
            "description": "A map of blessed key name to its per-protection ON|OFF state.",
            "additionalProperties": {
                "type": "object",
                "additionalProperties": {
                    "type": "string"
                }
            }
        }
    }
}
