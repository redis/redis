#define UNUSED(x) (void)(x)

unsigned int CustomkeyHashSlot(char *key, int keylen) {
	/* Add the custom hash function here 
	   Please remove the following code and replace it with your custom hash function. */
	UNUSED(key);
	UNUSED(keylen);
	return 0;
}
